export enum Role {
  SUPER = 'super',
  OPERATOR = 'operator',
  SUPPORT = 'support',
  TENANT = 'tenant',
  MERCHANT = 'merchant',
  CUSTOMER = 'customer',
  NONE = 'none',
}

export enum TokenType {
  JWT = 'jwt',
  API_KEY = 'api_key',
  NONE = 'none',
}
