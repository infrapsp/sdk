export enum FeeRuleEntity {
  TRANSACTION = 'transaction',
  TRANSFER = 'transfer',
}

export enum FeeRuleMethod {
  PIX = 'pix',
}

export enum FeeRulePayer {
  MERCHANT = 'merchant',
  TENANT = 'tenant',
}
