export enum InterTransferStatus {
  WAITING_APPROVAL = 'waiting_approval',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  REFUSED = 'refused',
}

export enum InterTransferDecision {
  APPROVED = 'approved',
  REFUSED = 'refused',
}
